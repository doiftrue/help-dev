version: "3.9"

services:

  nginx:
    container_name: "LOCAL_nginx"
    restart: always
    image: nginx:alpine
    volumes:
      - ${SERTS_DIR}:/var/SSL:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/_sites.conf:/etc/nginx/_sites.conf:ro
    network_mode: host
